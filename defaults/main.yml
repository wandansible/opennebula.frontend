---
one_datastores: []
one_hosts: []
one_vms: []

one_api_url: http://127.0.0.1:2633/RPC2
one_api_username: "{{ lookup('file', '~/.one/one_auth').split(':')[0] | trim }}"
one_api_token: "{{ lookup('file', '~/.one/one_auth').split(':')[1] | trim }}"
